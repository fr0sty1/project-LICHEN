# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: The contributors to the LICHEN project
#
<<<<<<< HEAD
# LilyGO T-Deck gateway config — ESP32-S3 + SX1262 + WiFi backhaul (border router mode)
# Resolved merge conflict project-LICHEN-m48r (worktree-worker23:t_deck_esp32s3_procpu.conf).
# Consolidated from worker19/21/23/24 changes (rh9u, etah, iwmd, fyph) and Heltec V3
# resolution (9ppq). Includes HAL, disabled hardware, bench provisioning (CRC32 MIC +
# hardcoded peers for T-Echo/T1000-E), large stacks to prevent overflows on L2 RX
# (Ed25519+SCHC), WiFi 6LBR with RPL root/prefix delegation, NVS epoch persist,
# ESP32-S3 WiFi buffer/heap tuning. SSID/PSK and insecure-MIC are bench-only.
# Update creds and keys before production use. Matches test/vectors/ and other boards.
=======
# LilyGO T-Deck gateway config — ESP32-S3 + SX1262

>>>>>>> origin/integration/worker2-20260722
CONFIG_LORA_SX126X=y
CONFIG_LICHEN_HAL=y
CONFIG_LICHEN_BOARD_NAME="LilyGO T-Deck"
CONFIG_LICHEN_HAS_LORA=y
CONFIG_LICHEN_RADIO_MODEL_SX126X=y
CONFIG_WIFI_NM=y
CONFIG_ESP_WIFI_ENABLED=y
CONFIG_LICHEN_HAS_BLE_LOCAL=n
CONFIG_LICHEN_HAS_SERIAL_LOCAL=n
CONFIG_LICHEN_HAS_DISPLAY=n
CONFIG_LICHEN_HAS_GNSS=n
CONFIG_LICHEN_HAS_BATTERY=n
CONFIG_LICHEN_HAS_PMIC=n
CONFIG_LICHEN_HAS_EXTERNAL_FLASH=n
CONFIG_SLIP=n
<<<<<<< HEAD
CONFIG_LICHEN_LINK_INSECURE_CRC32_MIC=y
CONFIG_LICHEN_L2_DEV_PROVISIONING=y
CONFIG_LICHEN_L2_DEV_PEER_EUI64="7a7ff09dc86c2c10,8a1a2c4b93812cab"
CONFIG_NET_TC_TX_COUNT=1
CONFIG_LICHEN_LORA_L2_RX_STACK_SIZE=12288
CONFIG_NET_RX_STACK_SIZE=8192
CONFIG_NET_TX_STACK_SIZE=12288
CONFIG_MAIN_STACK_SIZE=8192
CONFIG_LICHEN_LORA_L2_RX_TIMEOUT_MS=1000
CONFIG_LICHEN_LORA_TX_POWER=2
CONFIG_LICHEN_GATEWAY_RX1_DELAY_MS=100
=======

# Run net TX on a dedicated traffic-class thread. With TC_TX_COUNT=0, TX runs
# in the caller's context — boot-time ND/MLD sends run on the system workqueue,
# where lora_send() blocks waiting for the SX126x TX-done… which the driver
# delivers via a work item on that same workqueue. The self-deadlock times out
# after 2x airtime.
CONFIG_NET_TC_TX_COUNT=1

# RX-path stacks: the LoRa L2 RX thread runs Monocypher Ed25519 verify +
# SCHC decompress (multi-KB stack), and the net RX thread carries the
# decompressed packet through IPv6/UDP/socket delivery. The 2048/1500
# defaults overflow and crash on the first real received frame.
CONFIG_LICHEN_LORA_L2_RX_STACK_SIZE=8192
CONFIG_NET_RX_STACK_SIZE=4096
CONFIG_NET_TX_STACK_SIZE=8192
CONFIG_MAIN_STACK_SIZE=8192

# Persist the link TX epoch across reboots (to avoid replay coin-flip on
# gateway reboot). Settings-on-NVS backed by the board's storage_partition.
>>>>>>> origin/integration/worker2-20260722
CONFIG_FLASH=y
CONFIG_FLASH_MAP=y
CONFIG_NVS=y
CONFIG_SETTINGS=y
CONFIG_SETTINGS_NVS=y
CONFIG_LICHEN_LINK_EPOCH_PERSIST=y
<<<<<<< HEAD
CONFIG_LORA_LICHEN_GATEWAY_RPL_ROOT=y
CONFIG_LICHEN_GATEWAY_PREFIX_DELEGATION=y
CONFIG_LICHEN_GATEWAY_WIFI_SSID="lichen-test"
CONFIG_LICHEN_GATEWAY_WIFI_PSK="lichen123"
CONFIG_WIFI=y
CONFIG_NET_L2_ETHERNET=y
CONFIG_NET_L2_WIFI_MGMT=y
CONFIG_NET_IF_MAX_IPV6_COUNT=3
CONFIG_NET_IPV6_ND=y
CONFIG_NET_CONFIG_NEED_IPV6_ROUTER=y
CONFIG_NET_MAX_ROUTERS=4
CONFIG_NET_IPV6_NBR_CACHE=16
CONFIG_NET_BUF_RX_COUNT=20
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM=8
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=32
CONFIG_ESP_WIFI_STATIC_TX_BUFFER_NUM=8
CONFIG_HEAP_MEM_POOL_SIZE=131072
=======

CONFIG_LICHEN_GATEWAY_RX1_DELAY_MS=100
>>>>>>> origin/integration/worker2-20260722
