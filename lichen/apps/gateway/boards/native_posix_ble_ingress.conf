# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: The contributors to the LICHEN project

# Dedicated config for native_posix / native_sim BLE ingress (LCI + ble_ipsp_transport)
# Enables LORA_LICHEN_BLE paths while disabling link layer (no LICHEN_LINK, no LORA_L2).
# Prevents link.35.6 build issues. Use with lichen/apps/gateway and -b native_sim/native/64.
# Via EXTRA_CONF_FILE=boards/native_posix_ble_ingress.conf or OVERLAY_CONFIG.

CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y
CONFIG_BT_MAX_CONN=1
CONFIG_BT_DEVICE_NAME="LICHEN-BLE-INGRESS"
CONFIG_BT_HCI=y
CONFIG_BT_HCI_RAW=n

CONFIG_LORA_LICHEN_BLE=y
CONFIG_LICHEN_BLE_TRANSPORT=y
CONFIG_LICHEN_BLE_SLIP=y
CONFIG_LICHEN_BLE_TRANSPORT_REQUIRE_SECURE=n

# Stub/disable full link layer and LoRa (avoids link layer deps on native_posix)
CONFIG_LICHEN=y
CONFIG_LICHEN_LINK=n
CONFIG_LICHEN_LORA_L2=n

# BLE/LCI specific networking (NET_L2_DUMMY selected by LORA_LICHEN_BLE; keep gateway routing)
CONFIG_NET_L2_DUMMY=y
CONFIG_NET_IF_MAX_IPV6_COUNT=2
CONFIG_NET_PKT_RX_COUNT=16
CONFIG_NET_PKT_TX_COUNT=16
CONFIG_NET_BUF_RX_COUNT=32
CONFIG_NET_BUF_TX_COUNT=32

# Native sim support
CONFIG_ENTROPY_GENERATOR=y
CONFIG_TEST_RANDOM_GENERATOR=y
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_MAIN_STACK_SIZE=4096

# C Safety Policy - spec/appendix-c-safety.md
CONFIG_STACK_CANARIES=y
CONFIG_STACK_SENTINEL=y
CONFIG_ASSERT=y
CONFIG_ASSERT_VERBOSE=y
