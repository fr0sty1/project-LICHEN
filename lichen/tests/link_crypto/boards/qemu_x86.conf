# SPDX-License-Identifier: GPL-3.0-or-later
# Ztest console only. NETWORKING is enabled for Kconfig dependencies, but
# the QEMU SLIP networking backend needs a host slip daemon (fails on
# macOS runners); CONFIG_NET_TEST suppresses it (see zephyr cmake/emu/qemu.cmake).
CONFIG_NET_TEST=y
