{
  "id": "project-LICHEN-worker6-z63v.4",
  "title": "advance_holdoff underflows if called when holdoff_counter is 0",
  "description": "**File:** rust/lichen-rpl/src/multi_instance.rs\n**Lines:** 877-891\n\n**Issue:** `advance_holdoff()` checks `is_in_holdoff()` before decrementing, but the method's contract doesn't prevent callers from ignoring the return value and calling repeatedly:\n\n```rust\npub fn advance_holdoff(\u0026mut self) -\u003e bool {\n    if !self.is_in_holdoff() {\n        return false;\n    }\n\n    self.holdoff_counter -= 1;  // Could underflow if called without checking\n    // ...\n}\n```\n\nWhile the current code is safe because of the early return, the pattern is fragile:\n- Debug builds: would panic on underflow if check removed\n- Release builds: would wrap to 255, incorrectly restarting holdoff\n\n**Trigger:** Currently impossible due to early return, but refactoring could break invariant.\n\n**Fix:** Use saturating_sub for defense in depth:\n```rust\nself.holdoff_counter = self.holdoff_counter.saturating_sub(1);\n```",
  "status": "open",
  "priority": 2,
  "issue_type": "task",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-08-16T19:21:07.751968Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-08-16T19:21:07.751968Z",
  "labels": [
    "correctness",
    "edge-case",
    "review"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-z63v.4",
      "depends_on_id": "project-LICHEN-worker6-z63v",
      "type": "parent-child",
      "created_at": "2026-08-16T19:21:08.074784Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
