{
  "id": "project-LICHEN-worker6-9098.26",
  "title": "radio.py: record_collision omits channel/time_us; per-channel counts stay empty",
  "description": "## Problem\n\n`Metrics.record_collision` optionally takes `channel` and `time_us`. Those fill `_collisions_by_channel` and `_time_series`. `snapshot()` / `export_csv` / dashboard expose per-channel collisions.\n\nThe only production caller is `RadioMixin._get_rx_result_internal`:\n\n    self._metrics.record_collision(node_id, tx_ids)\n\nIt already has `channel = node.get_hop_channel()` and `self._current_time_us` in scope and passes `time_us` to the debug log and `on_collision` observer — but not to Metrics.\n\nResult: after a real radio collision, `metrics.collisions \u003e= 1` and `collisions_by_node` increment, but `collisions_by_channel` is `{}` and CSV has no collision rows. `test_metrics.py` `test_collision_without_channel` documents the empty-channel path; `test_radio.py` `test_callback_collision_updates_metrics_and_observer` only checks `collisions == 1`. Hopping / CCP-12 collision isolation cannot be observed from Metrics.\n\n## Files\n\n- python/src/lichen/sim/simulation/radio.py (`record_collision` call ~L626)\n- python/src/lichen/sim/metrics.py (`record_collision` channel/time_us kwargs)\n- python/tests/sim/test_radio.py (`test_callback_collision_updates_metrics_and_observer`)\n\n## Why this is robustness\n\nPer-channel collision counts are how hopping is validated under load. Always-empty `collisions_by_channel` is false confidence: total collisions rise while the channel breakdown used for hop-schedule debug stays zero.",
  "design": "Pass channel=channel and time_us=self._current_time_us into record_collision from _get_rx_result_internal (polling and callback share that path). Keep record_collision kwargs optional for unit tests that omit them. Assert collisions_by_channel[rx_hop] == 1 in the radio collision test.",
  "acceptance_criteria": "A two-TX callback/polling collision increments metrics.collisions_by_channel[receiver hop channel] and appends a collision row with time_us to export_csv. collisions_by_node still increments. test_collision_without_channel (direct Metrics call, no channel) still allows empty by_channel.",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "assignee": "grok",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-08-24T06:24:14.01353Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-08-24T06:41:58.480599Z",
  "started_at": "2026-08-24T06:26:00.34746Z",
  "closed_at": "2026-08-24T06:41:58.480599Z",
  "close_reason": "record_collision from radio.py now passes channel and time_us so collisions_by_channel and CSV time-series rows populate.",
  "labels": [
    "robustness",
    "review",
    "ultracode"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-9098.26",
      "depends_on_id": "project-LICHEN-worker6-9098",
      "type": "parent-child",
      "created_at": "2026-08-24T06:24:14.459507Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    },
    {
      "issue_id": "project-LICHEN-worker6-9098.26",
      "depends_on_id": "project-LICHEN-worker6-9098.22",
      "type": "discovered-from",
      "created_at": "2026-08-24T06:24:14.811141Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
