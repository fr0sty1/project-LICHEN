{
  "id": "project-LICHEN-worker6-awfz",
  "title": "[review] project-LICHEN-worker6-3k09: const violation writing dtn_buf through const pointer",
  "description": "In `lichen/subsys/lichen/coap/coap_dtn.c`, commit 443fe290f5 added line `s_provider-\u003edtn_buf = \u0026s_dtn_buf;` in `lichen_coap_deaddrop_register()`. However, `s_provider` is declared as `static const struct lichen_deaddrop_provider *s_provider` (line 23). Writing to a member through a `const`-qualified pointer is a C constraint violation (C11 §6.5.16: assignment requires a modifiable lvalue). This will fail to compile with `-Werror` or any conforming C compiler.\n\n**Fix options:**\n1. Remove `const` from `s_provider` declaration\n2. Set `dtn_buf` before the register call (have caller set it before calling register, and read it inside register)\n3. Pass dtn_buf as a separate parameter\n\n**Recommendation:** Remove `const` from `s_provider` since the struct contains mutable runtime state (dtn_buf). The function pointers (store/retrieve) are effectively immutable after registration.\n\n**Other observations in this commit:**\n- SPDX headers added (good)\n- Extensive 80-column reformatting (style, no functional change)\n- Confessions endpoint lacks authorization for unprotected requests (pre-existing, not introduced by this commit)",
  "status": "open",
  "priority": 1,
  "issue_type": "bug",
  "owner": "agent@lichen-project.dev",
  "created_at": "2026-07-24T19:44:58.871477808Z",
  "created_by": "AI Agent",
  "updated_at": "2026-07-24T19:44:58.871477808Z"
}
