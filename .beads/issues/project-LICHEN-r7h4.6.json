{
  "id": "project-LICHEN-r7h4.6",
  "title": "SX1262.cs Renode RX is one-shot; needs continuous RX polling + RxDone IRQ",
  "description": "During Renode nRF52840 validation (project-LICHEN-r7h4.5), LoRa TX to lichen-sim was confirmed working (transmissions recorded with correct airtime), but firmware never receives frames. Root cause: SX1262.cs PollRx() is only invoked when the firmware issues the SetRx (0x82) opcode. The loramac-node/sx126x driver issues SetRx once to enter continuous RX, then waits for a DIO1 RxDone interrupt. SX1262.cs never polls lichen-sim again, so any packet arriving after the single SetRx poll is never delivered and no RxDone IRQ is raised. Fix: while the radio is in RX mode, poll the sim periodically (Renode timer / IHasOwnLife background loop) and raise the RxDone IRQ (DIO1) when a packet arrives, then stop on ClearIrqStatus/SetStandby. Must handle the sim time-model interaction (see sibling bead).",
  "acceptance_criteria": "2-node Renode mesh in REALTIME sim mode shows receptions\u003e0 and the receiving firmware logs/processes an inbound frame; RX validated with 3 codereview passes.",
  "notes": "Superseded by push-based RX design in r7h4.7. The polling approach (continuous RX poll loop in SX1262.cs) is no longer needed - interrupt-driven push from sim handles RX correctly and matches real hardware behavior. Close this bead once r7h4.7 lands.",
  "status": "open",
  "priority": 2,
  "issue_type": "bug",
  "assignee": "Mark Atwood",
  "owner": "claude@ec2.local",
  "created_at": "2026-07-07T02:32:53.151311494Z",
  "created_by": "Claude Code (EC2)",
  "updated_at": "2026-07-10T18:33:29.703655Z",
  "closed_at": "2026-07-10T18:33:29.703655Z",
  "close_reason": "Superseded by push-based RX design in r7h4.7. Polling approach abandoned in favor of interrupt-driven RX_PACKET push from sim, which matches real SX1262 hardware behavior.",
  "dependencies": [
    {
      "issue_id": "project-LICHEN-r7h4.6",
      "depends_on_id": "project-LICHEN-r7h4",
      "type": "parent-child",
      "created_at": "2026-07-07T02:32:53.151538754Z"
    }
  ]
}
