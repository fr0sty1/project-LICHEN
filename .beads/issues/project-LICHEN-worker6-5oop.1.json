{
  "id": "project-LICHEN-worker6-5oop.1",
  "title": "Signer IID bit check uses reserved bit 0x80 instead of actual SI bit",
  "description": "Location: rust/lichen-link/src/schnorr.rs line 81\n\nIn build_signable(), the condition `llsec \u0026 0x80 != 0` checks bit 7 (reserved, must be 0 per spec 4.1) to determine whether to include signer_iid. Per lib.rs documentation, bit 7 is reserved and must always be 0, meaning this code path never executes.\n\nThe signer_iid parameter is passed through sign_frame() and verify_frame() but never actually included in the signed message because the reserved bit is never set. When SI (Signer IID) feature is properly specified and implemented, this dead code path could lead to signature malleability or verification failures if the wrong bit is used.\n\nTrigger: Currently no impact (dead code). Future SI feature implementation using wrong bit position would cause signature verification failures or allow unsigned signer_iid spoofing.\n\nSuggested fix: Either remove the signer_iid parameter until SI feature is properly specified, or document which LLSec bit will indicate SI presence and update the check accordingly.",
  "status": "closed",
  "priority": 2,
  "issue_type": "bug",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-08-16T18:49:47.581807Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-08-24T02:13:20.921684Z",
  "closed_at": "2026-08-24T02:13:20.921684Z",
  "close_reason": "Per draft-lichen-link-01.md section 3.2 the SI bit IS LLSec bit 7 (0x80): 'Bit 7 (SI): Signer IID present', and section 4.1 places SIID(8) in signed_data when SI=1 - so the existing mask was spec-correct but undocumented and untested dead-looking code (mainline spec/02-physical-link.md section 4.2 + reserved_bit7_reject vector still reserve bit 7 at parse time). Fixed via option (b): added pub const LLSEC_SI_BIT with spec citations, used it in build_signable, expanded sign_frame/verify_frame docs on IID coverage conditional on the bit. New tests: SI set covers signer_iid (wrong IID fails), SI clear ignores signer_iid (deterministic signatures identical, any IID verifies), and SI-frame serialize+verify roundtrip documenting that LichenFrame serialization does not yet carry the SI bit.",
  "labels": [
    "security",
    "crypto-security",
    "review"
  ],
  "dependencies": [
    {
      "issue_id": "project-LICHEN-worker6-5oop.1",
      "depends_on_id": "project-LICHEN-worker6-5oop",
      "type": "parent-child",
      "created_at": "2026-08-16T18:49:47.948879Z",
      "created_by": "Mark Atwood",
      "metadata": "{}"
    }
  ]
}
