{
  "id": "project-LICHEN-kgzk",
  "title": "Rust: EDHOC edhoc_kdf returns Result but original code used expect()",
  "description": "In rust/lichen-oscore/src/edhoc.rs, edhoc_kdf was changed from returning heapless::Vec directly to returning Result<heapless::Vec, EdhocError>. While this is correct, the refactor changed:\n\n    // Before: Silent truncation on overflow\n    okm.resize(length, 0).expect('length <= 32');\n    hk.expand(&info, &mut okm).expect('length <= 255');\n    okm\n\n    // After: Returns error on overflow\n    okm.resize(length, 0).expect('output Vec<u8, 32> fits any KDF output <= 32');\n    hk.expand(&info, &mut okm).expect('requested length <= 32 is within HKDF-Expand limits');\n    Ok(okm)\n\nThe expect() calls still exist, meaning the function can panic. For a crypto function, panics are generally undesirable. The Result return type suggests callers expect errors, but panics will bypass the Result.\n\nLocation: rust/lichen-oscore/src/edhoc.rs:163-174\nRisk: Panic in crypto code path; callers think they handle errors but can still panic.\nFix: Remove expect() calls and propagate errors, or document when panics are impossible.",
  "status": "closed",
  "priority": 2,
  "issue_type": "task",
  "assignee": "Mark Atwood",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-07-06T17:25:51.554998Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-07-06T18:53:05.781771Z",
  "closed_at": "2026-07-06T18:53:05.781771Z",
  "close_reason": "Fixed by workflow",
  "labels": [
    "owner-reserved"
  ]
}
