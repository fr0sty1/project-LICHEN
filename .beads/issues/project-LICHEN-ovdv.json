{
  "id": "project-LICHEN-ovdv",
  "title": "lichen_link_set_epoch fails silently when lock fails",
  "description": "File: lichen/subsys/lichen/link/link_ctx.c\nLines: 246-256\n\nlichen_link_set_epoch() returns void and fails silently if seq_lock() fails:\n\n```c\nvoid lichen_link_set_epoch(struct lichen_link_ctx *ctx, uint8_t epoch)\n{\n    if (ctx == NULL) {\n        return;\n    }\n    if (seq_lock(ctx) != 0) {\n        return;  // Silent failure - caller cannot detect\n    }\n    ctx-\u003eepoch = epoch;\n    (void)seq_unlock(ctx);\n}\n```\n\nOn POSIX, pthread_mutex_lock can fail with EINVAL (uninitialized mutex) or EDEADLK (deadlock). If the lock fails, the epoch is not updated but the caller has no way to detect this.\n\nImpact: Caller believes epoch was set when it was not. Could lead to nonce reuse if epoch change is critical for key rotation.\n\nSeverity: Low - on Zephyr, k_mutex_lock with K_FOREVER always succeeds. On POSIX, lock only fails due to programming errors. However, for safety-critical code, silent failures should be avoided.\n\nFix: Change return type to int and return error code on failure.",
  "notes": "Commit: 426c2f4c4",
  "status": "closed",
  "priority": 3,
  "issue_type": "bug",
  "assignee": "AI Agent",
  "owner": "me@mark.atwood.name",
  "created_at": "2026-07-12T06:47:08.81974Z",
  "created_by": "Mark Atwood",
  "updated_at": "2026-07-22T23:26:26.010819337Z",
  "started_at": "2026-07-22T23:23:02.662341931Z",
  "closed_at": "2026-07-22T23:26:13.05804183Z",
  "close_reason": "Fixed: lichen_link_set_epoch now returns int with -EINVAL/-EIO on failure paths (already implemented + header updated); no silent failure possible. Matches patterns in next_tx/copy_identity. Related to 2auf.35.4.1 normalization."
}
