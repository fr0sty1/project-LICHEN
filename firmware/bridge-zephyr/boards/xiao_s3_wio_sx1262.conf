# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: The contributors to the LICHEN project
#
# RETIRED/BLOCKED per project-LICHEN-2u26.10, project-LICHEN-80ju, project-LICHEN-irj9, project-LICHEN-7qaj (m5m1.8 bridge audit / project-LICHEN-2u26): Seeed XIAO ESP32-S3 + Wio-SX1262.
# No canonical Zephyr board target or composite shield defined. xiao_s3_wio_sx1262 not a valid -b target.
# Classified FAIL; support removed. Proxy: heltec_wifi_lora32_v3_esp32s3_procpu or t1000_e_nrf52840.
# Fragment retained for reference only. See docs/firmware-board-capability-matrix.md.
#
# Resolved merge conflict for project-LICHEN-pzlk (worktree-worker18), project-LICHEN-xnwv (worktree-worker23), project-LICHEN-jmk8, project-LICHEN-znqu, project-LICHEN-lon7 (worktree-worker24), project-LICHEN-54kl, project-LICHEN-os3g: xiao_s3_wio_sx1262.conf.
# Consolidated parallel worker updates to retired board fragment; standardized SPDX header and comment. Matches heltec_wifi_lora32_v3_esp32s3_procpu.conf and t_deck_esp32s3_procpu.conf patterns.
#
# ESP32-S3 with native USB CDC for console (retained for reference).
#
# USB CDC serial console
CONFIG_USB_DEVICE_STACK=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# SPI for SX1262
CONFIG_LORA=y
CONFIG_SPI=y

# GPIO for SX1262 control pins and LED
CONFIG_GPIO=y
