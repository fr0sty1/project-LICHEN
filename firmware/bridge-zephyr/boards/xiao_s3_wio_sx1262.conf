# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: The contributors to the LICHEN project
#
# RETIRED/BLOCKED per project-LICHEN-2u26.10, project-LICHEN-80ju, project-LICHEN-irj9, project-LICHEN-7qaj, project-LICHEN-ndbx (m5m1.8 bridge audit / project-LICHEN-2u26): Seeed XIAO ESP32-S3 + Wio-SX1262.
# No canonical Zephyr board target or composite shield defined. xiao_s3_wio_sx1262 not a valid -b target.
# Classified FAIL in bridge audit (m5m1.8). Support removed. Use heltec_wifi_lora32_v3_esp32s3_procpu (or t1000_e_nrf52840) as proxy. Follows tbeam_supreme pattern (project-LICHEN-w8rd).
# Fragment retained for reference only. See docs/firmware-board-capability-matrix.md.
#
# Resolved merge conflict for project-LICHEN-mkb2 (worktree-worker17), project-LICHEN-6j8k (worktree-worker19), project-LICHEN-rpc8/s2tx (worker19), project-LICHEN-pzlk/43jm/6p4q/e4qx (worker18), project-LICHEN-xnwv/ohl2/8djo/t6un (worker23), project-LICHEN-jmk8, project-LICHEN-znqu (worker24), project-LICHEN-54kl, project-LICHEN-o8gh/dxzk (worker20), project-LICHEN-y2do (worker3): xiao_s3_wio_sx1262.conf.
# Consolidated parallel worker updates to retired board fragment; standardized SPDX header and comment. Matches heltec_wifi_lora32_v3_esp32s3_procpu.conf and t_deck_esp32s3_procpu.conf patterns. No duplicate resolution markers or dead code per AGENTS.md.
#
# ESP32-S3 with native USB CDC for console (retained for reference).

# USB CDC serial console
CONFIG_USB_DEVICE_STACK=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# SPI for SX1262
CONFIG_LORA=y
CONFIG_SPI=y

# GPIO for SX1262 control pins and LED
CONFIG_GPIO=y
