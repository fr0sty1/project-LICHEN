# SPDX-License-Identifier: GPL-3.0-or-later
# SPDX-FileCopyrightText: The contributors to the LICHEN project
#
# RETIRED/BLOCKED per project-LICHEN-2u26.10 (m5m1.8 bridge audit): Seeed XIAO ESP32-S3 + Wio-SX1262. No canonical Zephyr board/composite shield. xiao_s3_wio_sx1262 invalid -b target. Classified FAIL; support removed. Proxy: heltec_wifi_lora32_v3_esp32s3_procpu (tbeam_supreme pattern per w8rd). Fragment retained for reference only. See docs/firmware-board-capability-matrix.md.
#
# ESP32-S3 with native USB CDC for console (retained for reference).
#
# Resolved merge conflict for project-LICHEN-pzlk (worktree-worker18), project-LICHEN-xnwv (worktree-worker23), project-LICHEN-jmk8, project-LICHEN-znqu (worktree-worker24), project-LICHEN-54kl, project-LICHEN-o8gh (worktree-worker20), project-LICHEN-y2do (worker3): xiao_s3_wio_sx1262.conf.
# Consolidated parallel worker updates to retired board fragment; standardized SPDX header and comment. Matches heltec_wifi_lora32_v3_esp32s3_procpu.conf and t_deck_esp32s3_procpu.conf patterns.
#
# ESP32-S3 with native USB CDC for console (retained for reference).
#
# USB CDC serial console
CONFIG_USB_DEVICE_STACK=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# SPI for SX1262
CONFIG_LORA=y
CONFIG_SPI=y

# GPIO for SX1262 control pins and LED
CONFIG_GPIO=y
