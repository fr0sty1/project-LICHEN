# STALE/RETIRED per project-LICHEN-2u26.5: no canonical Zephyr target for LilyGO T-Beam bridge fragment. Retired per 2u26 audit.
# LilyGo T-Beam v1.2 board-specific config
#
# ESP32 uses UART0 for console (via CP2102/CH340 USB-serial chip)
# rather than native USB CDC like nRF52.

# Disable USB CDC (not applicable to ESP32)
CONFIG_USB_DEVICE_STACK=n
CONFIG_USB_CDC_ACM=n

# UART console
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# SPI for SX1262
CONFIG_LORA=y
CONFIG_SPI=y
